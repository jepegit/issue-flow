# Status — Issue #249: use parts of pstack

Branch: `cursor/249-pstack-3dc1` · PR: https://github.com/jepegit/issue-flow/pull/250 (#250, draft)

- [ ] Done

## What's done

- 2026-09-09: captured issue, plan written and accepted (`issue249_plan.md`).
- 2026-09-09: `/iflow-build` — plan implemented:
  - Nine pstack skills vendored verbatim (upstream `9bd4a82`, pstack v0.15.1) as
    `templates/skills/pstack_*/SKILL.md.j2` + `_pstack_LICENSE.txt`, generated by
    the new `00-tools/vendor_pstack.py` (`--check` reports drift).
  - `templating.py`: `PSTACK_SKILL_NAMES` / `PSTACK_NAME_TO_STEM` /
    `PSTACK_SKILL_DIRS` / `OPTIONAL_SKILL_DIRS` / `DEFAULT_SKILL_DIRS`; manifests
    emit optional stems only when the mode lists them; upstream folder names via
    `SKILL_OUTPUT_NAMES`.
  - `modes.py`: `"all"` expands to the default set only; `normalize_pstack_skills`,
    `pstack_stems`, `pstack_names_for`, `read_pstack_skills`; union in
    `resolve_mode(..., pstack_skills=...)`; `pstack_skills` in default config.
  - `config.py`: `Settings.resolve_pstack_skills` (config > `ISSUEFLOW_PSTACK_SKILLS`
    > none), passed into `resolve_mode`; render key `pstack_skills`;
    `seed_config_values` + `config add` guide/help updated.
  - `step_profiles.py`: vendored stems excluded from lifecycle directives.
  - Templates: gated "pstack skills" section in `rules/_body.md.j2`; soft nudges in
    close (blast-radius, unslop) and build (tdd), skills + commands.
  - Tests: templating / modes / config / update (+ fixture fixes for the
    default-vs-optional split); `verify_scaffold.py` gains group 4/5.
  - Docs: `docs/configuration.md` "pstack skills" + env row, `docs/index.md`,
    README + `docs/acknowledgements.md` rows, design doc
    `04-designs-and-guides/pstack-skills.md`, tools README index.
  - Verified: `uv run pytest` (689 passed), `uv run ruff check src/ tests/`,
    `uv run .issueflows/00-tools/verify_scaffold.py` (all groups pass),
    `vendor_pstack.py --check` (no drift).

## Remaining work

- `/iflow-close`: `HISTORY.md` bullet, optional version bump, mark PR ready.
