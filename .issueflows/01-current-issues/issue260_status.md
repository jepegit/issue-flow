# Issue #260 status

- [ ] Done

## Progress

- Captured; plan written.
- **Part A done (code):**
  - `issue-flow agent pr-sync` (dry-run / push / fail-fast / worktrees)
  - `/iflow-pr-sync` skill + command templates
  - Design doc `pr-queue-sync.md`; cleanup offer + rules blurb
  - Tests in `tests/test_pr_sync.py`
- **Tests:** `714 passed` in worktree `/home/jepe/scripting/issue-flow-260`.
- **PR #259:** MERGEABLE / CLEAN (rebase done earlier).

## Remaining

- Commit + push + PR for Part A (ask when ready).
- Part B `defer_changelog` (prevent) — same PR if wanted, else follow-up.
- After #259 merges: `/iflow-cleanup`.
- Close when Part A accepted (Part B optional).
