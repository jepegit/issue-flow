# Issue #181 — status

- [ ] Done

## What's done

- Plan confirmed (`iflow-issue`; branch+init offer; structured body; epic-anchor hint).
- Added `skills/iflow_issue/SKILL.md.j2` + `commands/iflow-issue.md.j2`.
- Registered in `templating.py` + `step_profiles.toml` (`reasoning`).
- Cross-links: `/iflow`, fix, epic, review; rules body; workflow doc; README.
- Design note: `.issueflows/04-designs-and-guides/create-non-epic-issue.md`.
- Tests + ruff green (`544` passed).

## Remaining work

- `/iflow-close` — changelog/status finalize, commit/push/PR polish if needed.
