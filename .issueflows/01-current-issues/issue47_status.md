# Issue #47 — status

- [ ] Done

## What's done

- Defined dirty conditions in `dirty-issueflows.md` + `tracking.audit_issueflows()`
- Added `issue-flow doctor`, `agent audit`, `agent repair`
- Scaffolded `/iflow-doctor` skill + command templates
- Tests for audit/repair + CLI

## Remaining work

- Run full test suite + ruff
- `/iflow-close` when ready to ship
