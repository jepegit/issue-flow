# Status — Issue #12

- [ ] Done

## What's done

- `/iflow-split` skill + command: confirm-gated 2–5 flat children, native
  sub-issue link, parent task list, park parent locally.
- `issue-flow agent sub-issue-add` wraps REST `sub_issue_id` JSON `--input`.
- Pick / issue / plan offer split or epic; never auto-create.
- Dispatcher, rules, workflow doc, README list the new off-path surface.
- Design doc `linked-sub-issues.md`; amended `create-non-epic-issue.md`.
- Tests: templating, gitutils integer JSON, CLI skip/dry-run/link.
- `uv run pytest` 606 passed; ruff clean.

## Remaining work

- `/iflow-close`: HISTORY Unreleased bullet, optional version bump, PR ready.
