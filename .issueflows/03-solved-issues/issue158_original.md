# Issue #158: update all issue flows in a workspace

Source: https://github.com/jepegit/issue-flow/issues/158

## Original issue text

If we have a workspace (and we have an issueflow workspace toml file), could we have a method that updates all the issueflows within that workspace? It is a bit tedious to cd into each repo and run issueflow update.
