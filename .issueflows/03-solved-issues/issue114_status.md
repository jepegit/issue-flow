# Issue #114 — Iterative small fixes

Interactive `/iflow-fix` session. Individual fixes are logged below and landed together via `/iflow-close`.

- [x] Done

## Iterative fixes log

- 2026-07-03 — Nudge agents to run `ruff check --fix` when ruff is present: updated `python-quality-tools.md.j2`, `/iflow-start`, and `/iflow-close` templates + tests; regenerated local scaffold.
- 2026-07-03 — Merged `origin/main` into branch (fast-forward) to pick up #84 `/iflow-archive` + `issue-flow agent archive`.
- 2026-07-03 — Document `/iflow-archive` and `issue-flow agent archive` in README (skills list, off-path commands, CLI synopsis, agent table, when-to-use, simple mode).
