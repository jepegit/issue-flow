# Status — Issue #363

- [x] Done

## What's done

- New `docs/how-to/sample-session.md`: #328 worked through in 7 steps, from `iflow pick` to cleanup after the merge. Each step shows what you type, what the agent did and printed (trimmed, not rewritten), and a note at each of the 4 confirmation points, plus the cleanup refusal while the PR was unmerged. It includes an excerpt of the real `issue328_plan.md` and a closing note that the next worktree reported `location: workspace` (the new #328 code).
- Nav: How-to guides → Everyday. Linked from Getting started (step 4 and Where to go next), the How-to index and "Work one issue end-to-end".
- Source record: `03-solved-issues/issue363_session_log.md` (the raw log written during the session).
- Started directly at the user's request (no pick survey, no plan-approval stop).
- Link check: 0 errors. `uv run pytest` passes.

## Remaining work

- None.
