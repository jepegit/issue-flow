# Status — Issue #139: /iflow-pick + /iflow epic awareness + stage gates

- [x] Done

## Checklist

- [x] iflow-pick skill + command: "Active epic" candidate tier via
      `agent epic-status` `next_candidates`; epic-membership ranking
- [x] Stage gate: `/iflow-cleanup` skill + command offer summary comment +
      next-stage publish on stage completion (offer only, never auto)
- [x] Tests: pick-epic-preference + cleanup-stage-gate (458 passed)
- [x] HISTORY; scaffold regenerated

## Notes

Placed the stage gate in `/iflow-cleanup` (post-merge, when the issue is
actually closed on GitHub) rather than `/iflow-close` (pre-merge).
