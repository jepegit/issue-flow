# Status — issue #20: status of issues

- [x] Done

## Done so far

Implemented a new read-only, off-path `/issue-status` slash command plus its
paired Agent Skill, following the confirmed plan (`issue20_plan.md`).

- **New command template** `src/issue_flow/templates/commands/issue-status.md.j2`
  — reports Context, Focus issue (lifecycle stage via the `/iflow` A–D logic),
  Parked work, Solved archive, Open GitHub issues (cross-referenced + tagged
  focus/parked/solved-locally/untracked), and a Summary line. Supports `local`
  (skip GitHub) and a hint arg. Read-only, off-path, degrades gracefully when
  `gh` is unavailable.
- **New paired skill** `src/issue_flow/templates/skills/issueflow_issue_status/SKILL.md.j2`
  — `name: issueflow-issue-status`, `disable-model-invocation: true`, mirrors
  the command.
- **Registered** both stems in `src/issue_flow/templating.py`: `"issue-status"`
  in `COMMAND_NAMES`, `"issueflow_issue_status"` in `SKILL_DIRS`.
- **Rules** `src/issue_flow/templates/rules/_body.md.j2` — added a brief line
  documenting the off-path `/issue-status` command (feeds AGENTS.md / .mdc / CLAUDE.md).
- **Docs** `src/issue_flow/templates/docs/issue-workflow.md.j2` — bumped command
  count (eleven → twelve), added command + skill table rows, a numbered section,
  the non-dispatch note, and the detours block entry.
- **README.md** — added command + skill to the scaffold tree, an off-path bullet,
  and the Agent Skills sentence.
- **Tests** `tests/test_templating.py` (manifest counts 27→29; codex skills 14→15,
  manifest 15→16; expected command/skill lists) and `tests/test_init.py`
  (new `test_init_creates_status_command_and_skill`).

## Verification

- `uv run pytest` — 147 passed.
- `uv run ruff check src/ tests/` — all checks passed.

## Remaining work

- None functionally. Ready to ship via `/issue-close`. The rendered copies under
  this repo's own `.cursor/` and `docs/` are regenerated by `issue-flow update`
  (source of truth is the templates edited above); refreshing them is optional.
