# Issue #255 — status

- [x] Done

## What's done

- CLI `worktree-add` / `worktree-list` / `worktree-remove`; home stays on default.
- `switchback` skips linked worktrees (`in_worktree: true`).
- Shared `_worktree_start.md.j2`; pick / issue / fix / close / cleanup + commands.
- Design doc `separate-workspaces.md` + workflow pointer.
- Tests: add/idempotent/dirty-remove/remove-by-number; switchback skip; template markers.
- `uv run pytest` (710 passed), ruff clean.
- Bumped to 0.4.13; HISTORY promoted; archived under `03-solved-issues/`.

## Remaining work

- None.
