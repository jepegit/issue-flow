# Status: #322 workspace bootstrap next-command hint

- [x] Done

## What's done

- Classify-only prints `next: issue-flow workspace bootstrap … --yes --default <first-scaffolded>`.
- `--yes` without `--default` names the proposed member in the error.
- `--default` help says it takes a folder name.

## Remaining work

- None.
