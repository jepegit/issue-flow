# Issue #113: The different steps in iflow requires different models

Source: https://github.com/jepegit/issue-flow/issues/113

## Original issue text

Add appropriate model and execution directives in the skills. For cursor, we might be able to tell it to use auto mode (check the web if that is feasible). 

The directive paragraph should have a descriptive header, for example:
### MODEL & EXECUTION DIRECTIVE

We should go through each step ("/iflow-init", "/iflow-plan", etc.) and decide on if we prioritize speed and economy over deep thinking / complex reasoning.

We should consider allowing to use github labels also. But not sure how to do it.

We should add it as a configurable option (in the config file).
