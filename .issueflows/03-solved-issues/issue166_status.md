# Issue #166 — status

- [x] Done

## What's done

- `gh_label_names` / `gh_label_create` in `gitutils.py`
- `ensure_managed_labels()` + `bootstrap_labels` config (default on) in `sync.py`
- 4 new tests in `test_sync.py`; full suite 497 pass
- README documents `bootstrap_labels`
- PR #167 opened

## Remaining work

- None
