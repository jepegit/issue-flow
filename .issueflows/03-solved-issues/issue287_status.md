# Status: #287 Registry + update-all

- [x] Done

## Current

`registry.toml` + `register` / `unregister` / `init` registration.
`update --all` skips missing and locked roots, updates the rest,
forwards `--force` / `--editor`.

## Remaining

None.
