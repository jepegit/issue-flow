# Status — Issue #353

- [x] Done

## What's done

- Replaced `iflow-graphify.net` → `graphify.net` in the templates (rules body, iflow-graphify command and skill, workflow doc), `README.md` and `docs/graphify.md`. Re-rendered `AGENTS.md`, `.cursor/rules/issueflow-rules.mdc`, `.cursor/skills/iflow-graphify/SKILL.md` and `docs/issue-workflow.md`.
- graphify LICENSE link → branch `v8`. lychee install URL → getting-started guide. PEP 440 → version-specifiers page.
- Essential test `test_no_mangled_graphify_domain` plus a registry row.
- Local lychee external run: 1843 OK, 0 errors. `uv run pytest` passes.

## Remaining work

- None.
