# Status: #324 opt-in *.code-workspace sync

- [x] Done

## What's done

- `workspace init|bootstrap --code-workspace` / `--code-workspace-path`.
- Adds missing member folders; keeps settings / extras unless `--force`.
- Ambiguous or invalid JSON: exit 1, no write.
- Docs: how-to/workspaces + CLI help.

## Remaining work

None.
