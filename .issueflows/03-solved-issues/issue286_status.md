# Status: #286 Per-repo lock flag

- [x] Done

## Current

Shipped `[issueflow] locked` (default false, project-only).
`config show|set` report it. `--global locked` refused. User-global
`locked` ignored. `ISSUEFLOW_LOCKED` wins for one process.

## Remaining

None.
