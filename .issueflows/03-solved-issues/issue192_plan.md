# Plan — #192

## Goal
Wire `auto_adversarial_loops` (default 2) through config/modes and bake into docs.

## Approach
Mirror `cycle_max_issues` int knob; bake into workflow doc; tests.

## Files
modes.py, config.py, cli/agent docs, issue-workflow.md.j2, tests.
