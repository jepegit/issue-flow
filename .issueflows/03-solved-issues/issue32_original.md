# Issue #32: add grill-me skill

Source: https://github.com/jepegit/issue-flow/issues/32

## Original issue text

It is sometimes best to use mattpocock's grill-me skill in the planning for more complicated issues. Implement this skill (it should be easily available on github) as one of the issueflow skills. Note, we might need to tweak the grill-me skill so that it fits our issue flow philosophy. Also, I think it is best to not include it in the standard mode by default. But we might want a way to "turn it on" during planning. And a way to include it in a mode if the users wants to (I think this has already been handled by how allow the users to create custom modes).
