# Issue #113 — status

- [x] Done

## What's done

- `step_profiles.toml` + `step_profiles.py` — packaged economy/reasoning defaults, project overrides.
- Config knobs: `step_directives`, `model_label_flows`, `deep_model_label`, `fast_model_label`, `[issueflow.step_profiles]`.
- Shared `_model_directive.md.j2` wired into all 16 lifecycle skills + command twins.
- `/iflow-pick` label-driven model profile announcement (v1).
- Docs, rules pointer, design note `step-model-directives.md`.
- Tests: `test_step_profiles.py`, config/templating coverage. Full suite passes (331 tests).
- Re-scaffolded `.cursor/skills/` via `issue-flow update`.

## Remaining work

- None.
