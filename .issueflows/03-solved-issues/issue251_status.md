# Issue #251 status

- [x] Done

## What's done

- Plan accepted (HISTORY skip for ops path; allow `.issueflows/` commits on default with confirm; defer review ops; primary token `ops`)
- Config: `ops_label` resolve/persist/bake
- `/iflow-ops` + close ops path; pick routing (ops beats yolo)
- Docs: `ops-no-pr.md`, rules/workflow/dispatcher
- Tests + verify_scaffold; pytest 697; dogfood update + graphify
- Close: `uv version --bump patch` → 0.4.11; HISTORY promote

## Remaining work

- None
