# Status — #296

- [x] Done

## Done

- `update --all --workspace` unions registry + nearest workspace members,
  unique by resolved path.
- Default `update --all` still registry-only. `--workspace` without `--all`
  errors.
- `workspace update` skips duplicate resolved member paths.
- Docs + tests.

## Remaining

None.
