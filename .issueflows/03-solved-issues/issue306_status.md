# Status: #306 pick → epic → publish → auto-all

- [x] Done

## What's done

- Plan accepted (`issue306_plan.md`): compose-only `/iflow-drive <N>`.
- Skill + command templates, stem registration, off-path lists, `drive-mode.md`.
- Tests for render, off-path, abort tokens, cleanup `-d` only / no A2.
- `uv run pytest` 791 passed; `uv run ruff check src/ tests/` clean.
- Version bump `0.5.4` → `0.5.5`.

## Remaining work

- None.
