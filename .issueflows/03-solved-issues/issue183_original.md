# Issue #183: renaming from start to build

Source: https://github.com/jepegit/issue-flow/issues/183

## Original issue text

We should rename the skill and command for starting building after the plan to "build", e.g. "iflow-start" -> "iflow-build"

This is more logical. And it also frees the name "start" for other uses. For example if we want to have a very small skill that is automatically loaded that registers stuff and preps the editor for issue-flow.

## Comments (curated summary)

- **Additional tasks**:
  - Also update the docs when implementing this rename.

_Note: this section is an interpretive summary of the comment thread, not a verbatim dump. Source comments: 1, last comment by @jepegit on 2026-07-19._
