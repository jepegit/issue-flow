# Status: #285

- [x] Done

## Done

- User-global `config.toml` path (XDG / `%APPDATA%`)
- Preference knobs: project > user-global > env > default
- `config show|set --global`; `mode` refused on `--global`
- Tests + `docs/configuration.md`

## Remaining

None.
