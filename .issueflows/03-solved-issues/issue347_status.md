# Status — Issue #347

- [x] Done

## What's done

- `docs/concepts.md`: commands/skills, lifecycle plus dispatcher table, `.issueflows/` layout plus group moves, on/off-path, confirmation points, and a glossary. The glossary covers every term in the spec: focus issue, off-path, dispatcher, parked, sweep, yolo, epic, stage, epoch, adversarial review, squash-landed, mode / skill level / noob, managed block, harness, worktree-first. It also covers command and on-path.
- Nav entry `Concepts`. Linked from Home (×2), Getting started (×2) and the How-to index.
- Link check: 2164 links, 0 errors. `uv run pytest` passes.

## Deviations from the spec

- Optional glossary tooltips not added (explicitly optional). Could come with a later polish issue.

## Remaining work

- None.
