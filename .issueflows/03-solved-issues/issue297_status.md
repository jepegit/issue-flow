# Status — #297

- [x] Done

## Done

- `issue-flow register --discover [START]` walks for `.issueflows/` trees
  (default depth 4, no symlink follow).
- Confirm unless `--yes`. Not hooked from init / update --all / workspace update.
- Docs + tests (two scaffolds + decoy + depth cap).

## Remaining

None.
