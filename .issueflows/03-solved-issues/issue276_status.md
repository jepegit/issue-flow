# Issue #276 status

- [x] Done

## What's done

- Captured 2026-09-17. Worktree `/home/jepe/scripting/issue-flow-276`
  on `276-update-skill-clobber`.
- Plan accepted (warn+skip, `--force` overwrites).
- `skill_ownership` + stamp store; `write_manifest_files` skips foreign
  skill dirs; prune skips foreign collisions; `update --force`.
- Tests: symlink / extras / stamp mismatch skip; ours still refresh;
  `--force` overwrites without following symlink target.
- Version bumped 0.4.19 → 0.4.20. Close.

## Remaining work

- None.
